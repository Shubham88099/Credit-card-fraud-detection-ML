# Credit-card-fraud-detection-ML
This project aims to detect fraudulent credit card transactions using machine learning techniques. The dataset is highly imbalanced — only a small fraction of transactions are fraudulent — so the project includes data preprocessing, resampling, model training, and evaluation steps to improve prediction accuracy.
